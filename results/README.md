# Results

Experiment outputs are intentionally excluded from the public repository.

The runner creates the path passed to `--output` and writes one JSON file
incrementally. Keep generated responses private unless your review process and
data policy explicitly permit sharing them.

